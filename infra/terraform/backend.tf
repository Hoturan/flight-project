# Remote state backend (optional for sandbox).
# Uncomment and fill in values to store Terraform state in GCS.

# terraform {
#   backend "gcs" {
#     bucket = "flight-terraform-state"
#     prefix = "flight-project"
#   }
# }